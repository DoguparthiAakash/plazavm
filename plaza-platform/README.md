# `plaza-platform`

Hardware inspection and host capability scanning crate.

---

## 🛠 Responsibilities

- Host Operating System, Architecture, CPU Cores, and Memory detection (`PlatformDetector`).
- GPU capability probing (CUDA, ROCm, Metal).
- Classification of host hardware into platform profiles (`HighPerformanceDesktop`, `ServerNode`, `LaptopWorkstation`).
