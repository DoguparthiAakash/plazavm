# `plaza-config`

Declarative configuration parsing and intent specification crate.

---

## 🛠 Responsibilities

- Parsers for `plaza.yaml` and `plaza.toml`.
- Declarative `WorkspaceConfig` schema validation.
- Workload `IntentConfig` specification.
