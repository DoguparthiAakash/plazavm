# `plaza-api`

Application composition root and high-level client API crate.

---

## 🛠 Responsibilities

- Thread-safe `BootstrapBuilder` composition root for dependency injection.
- Unifies Storage, Platform, Decision, Controller, Resource, and PluginHost into a single `PlazaContainer`.
