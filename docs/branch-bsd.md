# Branch: feature/bsd

## Scope
- Support BSD guest VMs (FreeBSD, OpenBSD, NetBSD).
- Focus on headless configurations since BSD is often used server-side.

## Merge Criteria
- FreeBSD 14 boots and obtains an IP via NAT.
- Console access functions correctly.
