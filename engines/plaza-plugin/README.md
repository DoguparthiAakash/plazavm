# `plaza-plugin`

Extensible plugin host and dynamic plugin lifecycle manager crate.

---

## 🛠 Responsibilities

- Dynamic plugin host (`PluginHost`).
- Plugin manifest parsing (`PluginManifest`).
- `RuntimePlugin` async trait interface.
