# `plaza-core`

The foundational crate of PlazaVM v2 providing common domain types, error definitions, and filesystem sandbox paths.

---

## 🛠 Responsibilities

- Standardized `PlazaError` error types.
- Platform filesystem paths via `plaza_core::paths` (`data_dir()`, `config_dir()`, `plugin_dir()`).
- Core domain primitive definitions.
